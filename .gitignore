# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Mac finder directory attributes files
.DS_Store

# Mattrax Database
mattrax.db

# Prevent HTTPS certs for devlopment being exposed
/certs